version: "3.7"

services:
  img-resizer-api:
    image: img-resizer-api:0.0.1
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./public:/app/public
    ports:
      - "8000:8000"

volumes:
  public: